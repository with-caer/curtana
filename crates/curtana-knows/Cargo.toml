[package]
name = "curtana-knows"
description = "Knowledge embedding, storage, and retrieval tools."
version = "0.1.0"
edition.workspace = true
repository.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
# Data encoding.
codas = { path = "../../../codas/codas", features = ["serde"] }
codas-flow = { path = "../../../codas/codas-flow" }
codas-macros = { path = "../../../codas/codas-macros", features = ["serde"] }

# Persistence.
duckdb = { version = "1.2.2", features = ["bundled"] }

# LLM inference (for embedding).
curtana-infers = { workspace = true }

# Manifest persistence.
toml = "0.8.22"

# Serialization (required by codas-macros generated code).
serde = { workspace = true }

# JSON parsing for tool arguments.
serde_json = "1"

# Async runtime.
tokio = { workspace = true }

# Logging.
tracing = { workspace = true }
