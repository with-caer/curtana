[package]
name = "curtana-knows"
description = "Persistent knowledge storage, embedding, and retrieval."
version = "0.1.0"
edition.workspace = true
repository.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
# Data encoding.
codas = { version = "0.5.1", features = ["serde"] }
codas-flow = "0.5.1"
codas-macros = { version = "0.5.1", features = ["serde"] }

# Persistence.
duckdb = { version = "1.2.2", features = ["bundled"] }

# LLM inference (for embedding).
curtana-infers = { workspace = true }

# Serialization (required by codas-macros generated code).
serde = { workspace = true }

# Async runtime.
tokio = { workspace = true }
